# GitLab Pages Configuration for IMPULSPACE Website
image: alpine:latest

pages:
  stage: deploy
  script:
    - mkdir _public
    - cp -r * _public/
    - mv _public public
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
